# Agents

## Where all 13 System Prompts are defined in Markdown format, and the corresponding Machine-Enforceable Output Contracts are defined in JSON format. Each agent has a matching contract schema that defines the expected output structure for that agent's tasks.
